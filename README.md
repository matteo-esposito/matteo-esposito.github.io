# matteo-esposito.github.io

Static personal site for <https://matteo-esposito.github.io/>.

This repository replaces the older Hugo-generated homepage with a single
hand-authored static page that GitHub Pages can serve directly from the `main`
branch root. There is intentionally no `CNAME` file so the site uses the free
GitHub Pages domain.
